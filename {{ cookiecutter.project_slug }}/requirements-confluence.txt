# Optional Confluence publishing support. Install only when using docs-confluence-publish.
# Kept separate because mkdocs-confluence-publisher/md2cf pins older rich/requests
# versions that can conflict with runtime dependencies.
mkdocs-confluence-publisher
